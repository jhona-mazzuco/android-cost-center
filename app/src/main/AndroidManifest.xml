<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jipsoft.trabalho_final">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Trabalhofinal">
        <activity android:name=".domain.activity.CostActivity" />
        <activity android:name=".domain.activity.CostCreateActivity" />
        <activity android:name=".domain.activity.CenterCreateActivity" />
        <activity android:name=".domain.activity.CenterActivity" />
        <activity android:name=".domain.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>